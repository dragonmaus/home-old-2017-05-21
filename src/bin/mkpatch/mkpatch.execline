#!/command/execlineb

forbacktickx file
{
	pipeline
	{
		find -s -x . -name *.orig
	}
	sed
	-e s/^\\.\\///
	-e s/\\.orig$//
}
import file
emptyenv
export LC_ALL C
export TZ UTC
diff -u ${file}.orig $file
